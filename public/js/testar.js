
/**
 * TEst rubrik. 
 */


/**
 * This function adds one to its input.
 * @param {number} input any number
 * @returns {number} that number, plus one.
 */
function addOne(input) {
    return input + 1;
}


/**
 * This function adds3 one to its input.
 * @param {number} input any number
 * @returns {number} that number, plus one.
 */
function addto(input) {
    return input + 1;
}

/**
 * This function adds3 one to its input.
 * @param {number} input any number
 * @returns {number} that number, plus one.
 */
function addtre(input) {
    return input + 1;
}


/**
 * This function adds one to its inputadsfadfadfd testar. 
 */
function addfyra(input) {
    return input + 1;
}